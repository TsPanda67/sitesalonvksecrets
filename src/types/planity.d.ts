interface Window {
  planity: {
    key: string;
    primaryColor: string;
    container?: HTMLElement;
    accountContainer?: HTMLElement;
    appointmentContainer?: HTMLElement;
    giftVoucherContainer?: HTMLElement;
    onlineShopContainer?: HTMLElement;
  };
}